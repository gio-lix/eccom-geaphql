import React from 'react';
import {NextPage} from "next";
import Layout from "../components/Layout";

const Cart: NextPage = () => {
    return (
        <Layout>
            cart
        </Layout>
    );
};

export default Cart;
